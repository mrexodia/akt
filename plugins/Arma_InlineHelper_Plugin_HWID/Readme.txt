This is a sample plugin for ArmaInlineHelper v0.07 and higher.

Every plugin should have two functions. One that returns the plugin name
and one that actually does something.

Just see main.cpp for more information.

PS: Install plugins by copying them in the "Plugins" directory.